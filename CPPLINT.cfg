set noparent # Don't search for parent CFG files

filter=-build/c++11 # Using <threads> from C++11 is not a problem for us

exclude_files=third_party # Don't lint third-party code

linelength=120 # Increase line length limit to 120, we're not in the 80s anymore
